diff -up nfs-utils-1.2.3/utils/mount/mount.c.orig nfs-utils-1.2.3/utils/mount/mount.c
--- nfs-utils-1.2.3/utils/mount/mount.c.orig	2018-04-05 14:35:43.122961631 -0400
+++ nfs-utils-1.2.3/utils/mount/mount.c	2018-04-05 14:37:01.176968557 -0400
@@ -59,6 +59,9 @@ int string;
 #define FOREGROUND	(0)
 #define BACKGROUND	(1)
 
+/* These flags need to be set for user mounts */
+#define MS_SECURE	(MS_NOEXEC|MS_NOSUID|MS_NODEV)
+
 static struct option longopts[] = {
   { "fake", 0, 0, 'f' },
   { "help", 0, 0, 'h' },
@@ -574,6 +577,9 @@ int main(int argc, char *argv[])
 
 	parse_opts(mount_opts, &flags, &extra_opts);
 
+	if (flags & (MS_USERS|MS_USER))
+		flags |= MS_SECURE;
+
 	if (uid != 0) {
 		if (!(flags & (MS_USERS|MS_USER))) {
 			nfs_error(_("%s: permission denied"), progname);
