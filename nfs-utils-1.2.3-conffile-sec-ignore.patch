diff -up nfs-utils-1.2.3/support/include/conffile.h.save nfs-utils-1.2.3/support/include/conffile.h
--- nfs-utils-1.2.3/support/include/conffile.h.save	2013-08-14 13:49:16.837627000 -0400
+++ nfs-utils-1.2.3/support/include/conffile.h	2013-08-14 13:52:39.645635000 -0400
@@ -54,7 +54,7 @@ extern int      conf_end(int, int);
 extern void     conf_free_list(struct conf_list *);
 extern struct sockaddr *conf_get_address(char *, char *);
 extern struct conf_list *conf_get_list(char *, char *);
-extern struct conf_list *conf_get_tag_list(char *);
+extern struct conf_list *conf_get_tag_list(char *, char *);
 extern int      conf_get_num(char *, char *, int);
 extern char    *conf_get_str(char *, char *);
 extern char    *conf_get_section(char *, char *, char *);
diff -up nfs-utils-1.2.3/support/nfs/conffile.c.save nfs-utils-1.2.3/support/nfs/conffile.c
--- nfs-utils-1.2.3/support/nfs/conffile.c.save	2013-08-14 13:49:19.995721000 -0400
+++ nfs-utils-1.2.3/support/nfs/conffile.c	2013-08-14 13:52:39.653630000 -0400
@@ -561,7 +561,7 @@ cleanup:
 }
 
 struct conf_list *
-conf_get_tag_list(char *section)
+conf_get_tag_list(char *section, char *arg)
 {
 	struct conf_list *list = 0;
 	struct conf_list_node *node;
@@ -575,6 +575,8 @@ conf_get_tag_list(char *section)
 	cb = LIST_FIRST(&conf_bindings[conf_hash (section)]);
 	for (; cb; cb = LIST_NEXT(cb, link)) {
 		if (strcasecmp (section, cb->section) == 0) {
+			if (arg != NULL && strcasecmp(arg, cb->arg) != 0)
+				continue;
 			list->cnt++;
 			node = calloc(1, sizeof *node);
 			if (!node)
diff -up nfs-utils-1.2.3/utils/mount/configfile.c.save nfs-utils-1.2.3/utils/mount/configfile.c
--- nfs-utils-1.2.3/utils/mount/configfile.c.save	2013-08-14 13:52:33.222439000 -0400
+++ nfs-utils-1.2.3/utils/mount/configfile.c	2013-08-14 13:52:39.658635000 -0400
@@ -286,7 +286,7 @@ conf_parse_mntopts(char *section, char *
 	char *nvalue, *ptr;
 	int argtype;
 
-	list = conf_get_tag_list(section);
+	list = conf_get_tag_list(section, arg);
 	TAILQ_FOREACH(node, &list->fields, link) {
 		/*
 		 * Do not overwrite options if already exists 
