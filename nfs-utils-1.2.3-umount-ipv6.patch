diff -up nfs-utils-1.2.3/support/nfs/nfs_mntent.c.orig nfs-utils-1.2.3/support/nfs/nfs_mntent.c
--- nfs-utils-1.2.3/support/nfs/nfs_mntent.c.orig	2011-09-01 10:02:42.188742000 -0400
+++ nfs-utils-1.2.3/support/nfs/nfs_mntent.c	2011-09-13 15:08:30.585808000 -0400
@@ -84,7 +84,9 @@ unmangle(char *s) {
 		if (*s == '\\' && isoctal(s[1]) && isoctal(s[2]) && isoctal(s[3])) {
 			*sp++ = 64*(s[1] & 7) + 8*(s[2] & 7) + (s[3] & 7);
 			s += 4;
-		} else
+		} else if (*s == '[' || *s == ']') /* strip IPV6 brackes */
+			s++;
+		else
 			*sp++ = *s++;
 	}
 	*sp = 0;
