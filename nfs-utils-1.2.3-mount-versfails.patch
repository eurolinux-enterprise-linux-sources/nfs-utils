diff -up nfs-utils-1.2.3/utils/mount/network.c.orig nfs-utils-1.2.3/utils/mount/network.c
--- nfs-utils-1.2.3/utils/mount/network.c.orig	2011-09-16 07:07:00.000000000 -0400
+++ nfs-utils-1.2.3/utils/mount/network.c	2011-09-16 07:08:23.000000000 -0400
@@ -1253,6 +1253,8 @@ nfs_nfs_version(struct mount_options *op
 				*version = tmp;
 				return 1;
 			}
+			nfs_error(_("%s: invalid value for 'vers=' option"),
+					progname);
 			return 0;
 		case PO_NOT_FOUND:
 			nfs_error(_("%s: parsing error on 'vers=' option\n"),
@@ -1270,6 +1272,8 @@ nfs_nfs_version(struct mount_options *op
 				*version = tmp;
 				return 1;
 			}
+			nfs_error(_("%s: invalid value for 'nfsvers=' option"),
+					progname);
 			return 0;
 		case PO_NOT_FOUND:
 			nfs_error(_("%s: parsing error on 'nfsvers=' option\n"),
