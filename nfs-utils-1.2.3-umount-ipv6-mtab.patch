diff -up nfs-utils-1.2.3/utils/mount/fstab.c.orig nfs-utils-1.2.3/utils/mount/fstab.c
--- nfs-utils-1.2.3/utils/mount/fstab.c.orig	2012-09-06 10:59:48.570169000 -0400
+++ nfs-utils-1.2.3/utils/mount/fstab.c	2012-09-06 11:22:05.278731000 -0400
@@ -542,6 +542,7 @@ update_mtab (const char *dir, struct mnt
 	const char *fnam = MOUNTED;
 	struct mntentchn mtabhead;	/* dummy */
 	struct mntentchn *mc, *mc0, *absent = NULL;
+	char *ptr, *tmp;
 
 	if (mtab_does_not_exist() || !mtab_is_writable())
 		return;
@@ -562,6 +563,16 @@ update_mtab (const char *dir, struct mnt
 
 	read_mntentchn(mfp, fnam, mc);
 
+	/*
+	 * Strip out any '[' and ']' ; a feature of mtab!
+	 */
+	if ((ptr = strchr(dir, '[')) != NULL) {
+		tmp = (ptr + 1);
+		memcpy(ptr, tmp, strlen(tmp)+1);
+	}
+	if ((ptr = strchr(dir, ']')) != NULL)
+		*ptr = '\0';
+
 	/* find last occurrence of dir */
 	for (mc = mc0->prev; mc && mc != mc0; mc = mc->prev)
 		if (streq(mc->m.mnt_dir, dir))
