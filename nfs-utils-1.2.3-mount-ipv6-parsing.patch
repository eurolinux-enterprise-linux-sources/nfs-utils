diff -up nfs-utils-1.2.3/support/nfs/nfs_mntent.c.orig nfs-utils-1.2.3/support/nfs/nfs_mntent.c
--- nfs-utils-1.2.3/support/nfs/nfs_mntent.c.orig	2015-05-19 10:39:13.036128000 -0400
+++ nfs-utils-1.2.3/support/nfs/nfs_mntent.c	2015-05-19 11:07:51.041974000 -0400
@@ -84,9 +84,7 @@ unmangle(char *s) {
 		if (*s == '\\' && isoctal(s[1]) && isoctal(s[2]) && isoctal(s[3])) {
 			*sp++ = 64*(s[1] & 7) + 8*(s[2] & 7) + (s[3] & 7);
 			s += 4;
-		} else if (*s == '[' || *s == ']') /* strip IPV6 brackes */
-			s++;
-		else
+		} else
 			*sp++ = *s++;
 	}
 	*sp = 0;
@@ -152,7 +150,7 @@ nfs_addmntent (mntFILE *mfp, struct mnte
 		res = fflush(mfp->mntent_fp);
 		if (res < 0)
 			/* Avoid leaving a corrupt mtab file */
-			ftruncate(fileno(mfp->mntent_fp), length);
+			(void)ftruncate(fileno(mfp->mntent_fp), length);
 	}
 	return (res < 0) ? 1 : 0;
 }
